# Coolify deployment: OpenTelemetry Collector for Docker host monitoring.
# Deploy one instance per Docker host. Sends telemetry to SigNoz via OTLP/HTTP
# (HTTPS on 443 through Cloudflare). SIGNOZ_OTLP_ENDPOINT must be a full https:// URL.
# Set SIGNOZ_HOST (+ optional SIGNOZ_INGESTION_KEY for Cloud) in Coolify env UI.
# Build pack: Docker Compose (uses this file by default). config.yaml is baked
# into the image via the Dockerfile — do NOT bind-mount it (Coolify remaps
# relative bind mounts to app storage where the file is missing, which fails).
# network_mode: host is required for host metrics, Docker stats/logs, and OTLP listeners.
# Apps on the same host send OTLP to localhost:4317 (gRPC) or localhost:4318 (HTTP).
# NOTE: Do not use YAML anchors (&/ *) — Coolify's compose preprocessor fails with
# "no service selected" on cold deploys (https://github.com/coollabsio/coolify/issues/2705).

services:
  signoz-collection-agent:
    build:
      context: .
      args:
        OTEL_COLLECTOR_VERSION: ${OTEL_COLLECTOR_VERSION:-0.139.0}
    image: signoz-collection-agent:${OTEL_COLLECTOR_VERSION:-0.139.0}
    container_name: signoz-collection-agent
    restart: unless-stopped
    user: "0:0"
    network_mode: host
    environment:
      # --- SigNoz connection (set in Coolify env UI) ---
      SIGNOZ_HOST: ${SIGNOZ_HOST:?Set SIGNOZ_HOST — your SigNoz server IP or hostname}
      SIGNOZ_OTLP_ENDPOINT: ${SIGNOZ_OTLP_ENDPOINT:-https://${SIGNOZ_HOST}}
      SIGNOZ_INGESTION_KEY: ${SIGNOZ_INGESTION_KEY:-}
      SIGNOZ_OTLP_INSECURE: ${SIGNOZ_OTLP_INSECURE:-true}
      OTEL_RESOURCE_ATTRIBUTES: deployment.environment=${DEPLOYMENT_ENVIRONMENT:-production}
      HOSTMETRICS_INTERVAL: ${HOSTMETRICS_INTERVAL:-60s}
      DOCKER_STATS_INTERVAL: ${DOCKER_STATS_INTERVAL:-10s}
      DOCKER_STATS_API_VERSION: ${DOCKER_STATS_API_VERSION:-1.44}
      FILELOG_START_AT: ${FILELOG_START_AT:-end}
      MEMORY_LIMITER_LIMIT_MIB: ${MEMORY_LIMITER_LIMIT_MIB:-4000}
      MEMORY_LIMITER_SPIKE_MIB: ${MEMORY_LIMITER_SPIKE_MIB:-800}
    volumes:
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /:/hostfs:ro
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:13133"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
