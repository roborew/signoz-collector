# Coolify / local env template. Full contract: see README.md.
#
# Production (Coolify): set INFISICAL_* bootstrap + DEPLOYMENT_ENVIRONMENT only.
# SigNoz connection secrets live in Infisical (SIGNOZ_OTLP_ENDPOINT, SIGNOZ_HOST,
# SIGNOZ_INGESTION_KEY). Locally: keep INFISICAL_* in .env, run `make env-pull`
# for secrets in .env.local.

# =============================================================================
# REQUIRED on Coolify — Infisical bootstrap
# =============================================================================

INFISICAL_PROJECT_ID=
INFISICAL_ENV=prod
INFISICAL_API_URL=https://eu.infisical.com
INFISICAL_CLIENT_ID=
INFISICAL_CLIENT_SECRET=
# INFISICAL_DOMAIN=   # alternative to INFISICAL_API_URL
# INFISICAL_TOKEN=    # alternative to client id/secret
# INFISICAL_USE_CLI=false  # escape hatch: skip Infisical, use SIGNOZ_* below

# =============================================================================
# REQUIRED on Coolify — per-host label (do NOT put in Infisical)
# =============================================================================

DEPLOYMENT_ENVIRONMENT=prod-coolify-host-01

# =============================================================================
# Infisical secrets (store in Infisical project — not Coolify)
# =============================================================================
# SIGNOZ_OTLP_ENDPOINT=https://otel.roborew.xyz
# SIGNOZ_HOST=otel.roborew.xyz
# SIGNOZ_INGESTION_KEY=

# Escape hatch when INFISICAL_USE_CLI=false (or local without Infisical):
# SIGNOZ_OTLP_ENDPOINT=https://otel.roborew.xyz
# SIGNOZ_HOST=otel.roborew.xyz
# SIGNOZ_INGESTION_KEY=

# =============================================================================
# OPTIONAL — Collector tuning (compose defaults; override only if needed)
# =============================================================================

# OTEL_COLLECTOR_VERSION=0.139.0
# HOSTMETRICS_INTERVAL=60s
# DOCKER_STATS_INTERVAL=10s
# DOCKER_STATS_API_VERSION=1.44
# FILELOG_START_AT=end
# MEMORY_LIMITER_LIMIT_MIB=4000
# MEMORY_LIMITER_SPIKE_MIB=800

# =============================================================================
# Application OTEL (set on *app* Coolify services — NOT on this collector)
# =============================================================================
# Apps on the same Docker host reach this host-networked collector via the host
# gateway. Add to each instrumented service:
#   extra_hosts: ["host.docker.internal:host-gateway"]
# and set:
#
#   OTEL_EXPORTER_OTLP_ENDPOINT=http://host.docker.internal:4318
#   OTEL_SERVICE_NAME=your-unique-service-name
#
# Do NOT set on apps:
#   - SIGNOZ_* (collector-only)
#   - OTEL_EXPORTER_OTLP_HEADERS (not needed for self-hosted via this collector)
#   - OTEL_EXPORTER_OTLP_ENDPOINT=https://otel.roborew.xyz (bypasses the collector)
#   - localhost:4318 (localhost inside a bridge container is not the host)
#
# See README.md "App project checklist".
