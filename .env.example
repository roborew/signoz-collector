# Coolify / local env template. Full contract: see README.md.
#
# Production (Coolify): set only the Infisical bootstrap block + DEPLOYMENT_ENVIRONMENT.
# Everything else (SIGNOZ_*, tuning) lives in Infisical — one lookup.
# Locally: keep INFISICAL_* in .env, run `make env-pull` for secrets in .env.local.

# =============================================================================
# REQUIRED on Coolify — Infisical bootstrap
# =============================================================================

INFISICAL_PROJECT_ID=
INFISICAL_ENV=prod
INFISICAL_API_URL=https://eu.infisical.com
INFISICAL_CLIENT_ID=
INFISICAL_CLIENT_SECRET=

# =============================================================================
# REQUIRED on Coolify — per-host label (keep out of Infisical so hosts differ)
# =============================================================================

DEPLOYMENT_ENVIRONMENT=prod-coolify-host-01

# =============================================================================
# Infisical secrets (manage here — not Coolify)
# =============================================================================
# Required:
#   SIGNOZ_OTLP_ENDPOINT=https://otel.roborew.xyz
#   SIGNOZ_HOST=otel.roborew.xyz
#   SIGNOZ_INGESTION_KEY=
#
# Optional tuning (omit to use config.yaml defaults; set to override):
#   HOSTMETRICS_INTERVAL=60s
#   DOCKER_STATS_INTERVAL=10s
#   DOCKER_STATS_API_VERSION=1.44
#   FILELOG_START_AT=end
#   MEMORY_LIMITER_LIMIT_MIB=4000
#   MEMORY_LIMITER_SPIKE_MIB=800

# =============================================================================
# Application OTEL (set on *app* Coolify services — NOT on this collector)
# =============================================================================
# Apps on the same Docker host reach this host-networked collector via the host
# gateway. Add to each instrumented service:
#   extra_hosts: ["host.docker.internal:host-gateway"]
# and set:
#
#   OTEL_EXPORTER_OTLP_ENDPOINT=http://host.docker.internal:4318
#   OTEL_SERVICE_NAME=your-unique-service-name
#
# See README.md "App project checklist".
